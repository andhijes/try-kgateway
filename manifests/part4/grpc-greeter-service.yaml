apiVersion: apps/v1
kind: Deployment
metadata:
  name: grpc-greeter
  labels:
    app: grpc-greeter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grpc-greeter
  template:
    metadata:
      labels:
        app: grpc-greeter
    spec:
      containers:
      - name: greeter
        image: alpine:latest
        ports:
        - containerPort: 50051
        command: ["/bin/sh"]
        args: ["-c", "apk add --no-cache socat && echo 'gRPC Greeter Service Mock' && socat TCP-LISTEN:50051,fork EXEC:'/bin/echo HTTP/2.0 200 OK'"]
        resources:
          requests:
            memory: "32Mi"
            cpu: "50m"
          limits:
            memory: "64Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: grpc-greeter-service
spec:
  selector:
    app: grpc-greeter
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
  type: ClusterIP